# Build output and packages.
dist/
release/
node_modules/
.claude/

# Generated data and the design toolchain; ladders.py writes the JSON and build.py the page.
design/
public/
package-lock.json

# Recorded simulator output: byte-for-byte fingerprints, never reformatted.
test/golden/

# Bundled fonts, and the symbol faces scripts/pip_symbols.py generates.
src/ui/fonts/
src/ui/pipfont/

# Prose is kept out of the formatter so a reflow never lands in a code diff.
*.md
*.html
